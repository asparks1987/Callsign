# Test Plan

## Purpose

`v1.0 alpha` is proven when the Free open-source core works end-to-end from install through visible launch.

All Alpha v1 features are free and remain free until at least beta.

## v1.0 automated coverage

Default smoke command:

```powershell
dotnet run --project tests/Callsign.AlphaSmoke/Callsign.AlphaSmoke.csproj
```

Required checks:

- fresh install/service startup assumptions
- profile creation and local settings persistence
- wake-only speech does not launch
- wake word detection and identity gate for `Callsign` / `call sign`
- enrollment sample proof includes fresh distinct hashes plus quality metadata for peak/RMS, duration, clipping ratio, and noise-oriented zero-crossing rate
- `callsign.gif` appears and session readout updates while active, including visible wake-overlay safety text for stop/cancel and identity-before-command behavior
- identity success and mismatch handling
- session-state invariant tests proving fresh identity authorization is valid only during active command/launch states and is cleared on completed, timed-out, cancelled, or locked-out terminal states
- visible Start menu launch path
- safe stop, cancel, timeout, and lockout handling
- runtime snapshot includes session state, transcript, overlay readout, and microphone telemetry
- runtime control diagnostics redact scripted transcript text and log only length/hash metadata
- runtime temporary audio cleanup deletes stale segment, wake-window, and wake-warmup WAV files while retaining only fresh diagnostics
- unsafe launch text, paths, URLs, shell fragments, secret-like inputs, and administrative/elevated tooling are rejected

## Manual v1.0 walkthrough

1. Build and install from clean state.
2. Create profile and callsign.
3. Record and review voice samples, including visible rejection of clipped, silent/too-quiet, or excessively noisy samples before enrollment.
4. Train or mark voice identity enrolled.
5. Confirm service/runtime health in the UI.
6. Speak wake phrase and confirm overlay + live text, including visible stop/cancel/reset safety text and the identity-before-command reminder.
7. Speak callsign and confirm identity acceptance.
8. Speak launch command.
9. Confirm Start menu action is visible.
10. Validate wrong identity and no-mic cases do not launch.
11. Confirm Getting Started reopens the walkthrough from the Account tab, exposes accessible names/descriptions for its guidance, safety and tier summary, visible close button, navigation controls, and the visible step/current badges, and can jump to Shortcuts, Help, Updates, and Packs for local shortcut, command discovery, release proof, manual evidence, and extension management. Confirm the Help tab includes direct visible pack-management shortcuts for Import Pack, Import Folder, Drop DLL Folder, Open Packs Folder, Refresh Packs, Update Pack, Rollback Pack, Enable Pack, Disable Pack, and Remove Pack. Confirm the Account and Help tabs also expose direct Open Manual Evidence, Open Checklist, and Open Release Evidence routes so the clean-install proof stays reachable from the entry surfaces. Confirm the walkthrough release-proof step and voice cue explicitly name the accessibility visual audit inside the manual checklist so Apple-style visual proof stays visible before release. Confirm the summary names the Free alpha parity core, the visible stop/cancel path, and community/Pro/Advanced pack gates before enablement. Confirm the Account and Help tabs expose the visible visual-status readback route, and that `read visual status`, `read accessibility mode`, `read high contrast status`, `read text scale status`, and `read reduced motion status` read the macOS Voice Control visual target, accessibility mode with palette mode, bounded text scale, reduced-motion-safe state, evidence tokens, STOP affordance, browser-helper discovery, and Free open-core boundary locally. Confirm the Updates tab also shows the visible release-proof line that compares the local installer SHA-256 against the public download target, the visible website target line that names the public `/downloads/Callsign-Setup.exe` endpoint, the restart-proof line that states the update service reloads its saved state after restart, the restart-safe downloaded-installer path line that states the downloaded installer remains available after restart, the visible check-in-status readback line that reports the last phone-home time, the visible update-status line that also exposes the last downloaded installer path when one exists, and the release-evidence folder action that opens the generated parity evidence artifacts, including the walkthrough's direct Open Release Evidence button and the manual evidence template and checklist buttons.
12. Validate stop, cancel, timeout, and lockout behavior.

## Voice Access parity coverage

The v1.x parity line is proven against `VOICE_ACCESS_PARITY_MATRIX.md`.

Automated coverage must include:

- command parser and alias tests for every parity command family
- Start menu resolver tests proving exact/alias matches resolve and ambiguous app names require confirmation
- policy allow/deny/approval/fresh-identity tests by risk tier
- command pack load/import/drag-drop/disable/enable/remove tests, including normal Windows `.dll` filename validation for community packs
- service-side extension execution tests proving identity and fresh-identity proof are derived from the active session state machine before registry execution, never hard-coded for community or paid packs
- service-side built-in command execution tests proving browser, file-search, dictation, and system-control commands route only after built-in policy evaluation with session-derived identity/freshness; browser/file/dictation request their visible surfaces; safe keyboard, mouse, media, window, and settings actions route through `SystemControlService`; ambiguous named-window switching defers to visible numbered choices; approval-required system commands fail closed until visible System-tab approval; service-executed browser, file-search, dictation, system, UI, extension, and Start-menu actions write profile-scoped `alpha.service_command_execution` JSONL audit records with `service_runtime` source and `visible_status` verification; and service audit-write failures become visible `Audit warning` status/action-history guidance instead of being silently suppressed
- local voice-shortcut store/save/delete/enable/disable tests, resolvable-command validation, direct self-recursion and indirect-loop rejection, malformed local shortcut filtering, follow-up-step execution metadata tests, configured-wake-word follow-up routing tests, and background-service follow-up routing tests proving shortcut chains keep fresh identity only until the bounded visible chain completes
- command discovery tests for open-core and extension-pack commands
- update manifest parsing tests for splash-screen command deltas and feature highlights
- visible evidence-status readback tests for release evidence folder, release packet summary, parity evidence JSON, `release_ready`, blocker count, next action, manual evidence template, and manual checklist presence
- visible manual-evidence progress tests for the Updates tab `Manual`, `Remaining`, `Categories`, `Proof`, `Category`, and `Next` badges so missing live parity proof and the next proof target are visible before a parity claim
- visible release-blocker readback tests for `read release blockers`, `why is release blocked`, blocker count, manual-check count, missing-category count, automated-failure count, and next release action
- visible next-proof readback tests for `read next proof` and `what proof is next` so the first missing live proof item and category can be spoken without opening JSON
- visible visual-status readback tests for the macOS Voice Control visual target, shared evidence tokens, STOP affordance, browser-helper discovery, and Free open-core boundary
- runtime observability tests proving compact authority/status text reports stale runtime snapshots as unknown current service health instead of presenting old hearing/listening data as live, and proving the detailed Runtime proof/microphone-level text labels `snapshot=fresh` or `snapshot=stale` with restart/reconnect guidance before trusting stale microphone state
- Free parity versus paid-pack gating evidence tests proving the `Free Parity` command view contains only Free Open Core parity commands while paid Pro/Advanced commands remain discovery-only, entitlement-required, and non-routing without entitlement
- update phone-home payload tests proving raw profile callsign/account ids and raw local update device ids are hashed before POST
- dictation review/edit/correction tests, including previous/next word, sentence, and paragraph movement, previous/next word and sentence selection/deletion, spoken target-text and range-based select/delete/replace inside the visible review buffer, delete-all, tab, sentence/line/paragraph break commands, and undo/redo recovery snapshots in the visible review buffer
- dictation capture-bound tests proving local and service-fed dictation stop at the shared 10-minute, 12,000-character, and 128-service-segment limits while preserving visible review text
- dictation punctuation and symbol command tests for quotes, parentheses, brackets, braces, dash, slash, backslash, pipe, backtick, tilde, underscore, plus, equals, number sign, dollar sign, ampersand, percent, caret, asterisk, and at sign
- dictation case-formatting parser, casing-mode command, and review-buffer transformation tests
- correction-alternative parser and review-buffer replacement tests
- visible controls, foreground-app UI Automation label normalization, compact numbered-overlay layout, visible numbered-control safety text, numbered click/double-click/triple-click/right-click/toggle/expand/collapse/slider routing, and browser page-find/private-window/bookmark/print/save/scroll/start-scroll/stop-scroll/system/file command routing tests
- service-side browser action tests proving every `browser-*` target routes through the visible browser action path before URL/search fallback, including find text, address text, tab selection, page scroll, zoom, private-window, save, print, downloads, and history actions
- Task View and virtual desktop routing tests
- visible projection/cast panel routing tests that open Windows panels without selecting projection mode or connecting to a wireless display
- approval-gated clipboard-history routing, clipboard privacy metadata, and explicit approval tests with no clipboard-content inspection
- approval-gated snipping-toolbar routing, screenshot privacy metadata, and explicit approval tests with no screenshot capture/read/save/upload
- safe Windows Settings page routing tests that preserve plain `open Settings` as Start menu launch behavior and cover display, sound, Bluetooth, Wi-Fi, network, accessibility, mouse, keyboard, privacy, power, installed apps, default apps, date/time, notifications, Windows Update, and personalization pages
- accessibility subpage routing tests for Magnifier, Narrator, Captions, and Speech settings that open visible Settings pages without toggling assistive features
- visible Magnifier accessibility-surface routing tests for open, zoom out, and close commands
- safe media-key routing tests for play/pause, next, previous, and stop playback
- keyboard keypress and natural editing/formatting/document/zoom shortcut routing tests for Space, letter keys, number-row keys, symbol keys, Delete, Insert, Windows key, context menu key, Caps Lock, F1-F12, repeated single-key phrases such as `press tab five times` and `press down three times`, Voice Access-style repeated arrow movement such as `move down two times` and `go right four times`, Voice Access-style selected-text boundary movement such as `move to beginning of selection` and `go to end of selection`, selected-text clearing with `unselect that` and `clear selection`, current word-boundary movement such as `go to beginning of word` and `move to end of word`, current-unit selection/deletion/copy/cut with `select word`, `select line`, `select paragraph`, `delete word`, `delete line`, `delete paragraph`, `copy word`, `copy line`, `copy paragraph`, `cut word`, `cut line`, and `cut paragraph`, previous/next copy-cut aliases such as `copy previous word`, `copy next line`, and `cut previous paragraph`, counted directional navigation such as `move left five characters`, `move right three words`, `go down three lines`, and `go back two paragraphs`, counted character, word, line, and paragraph copy/cut such as `copy previous three characters`, `copy last four characters`, `copy backward four characters`, `copy forward six letters`, `copy previous three words`, `copy next five words`, `copy previous three lines`, `copy backward three lines`, `copy next five lines`, `copy previous three paragraphs`, `copy next three paragraphs`, `cut forward six letters`, `cut forward four lines`, `cut next five words`, `cut next five lines`, and `cut next three paragraphs`, active-app unit formatting such as `bold previous word`, `italicize next line`, `underline paragraph`, and `make last paragraph underlined`, `dismiss` as a visible Escape alias, modifier chords (including tab/home/end and allowed Control/Alt/Shift combinations), held Shift/Control/Alt modifier commands with release-all safety, active-app character, line, word, sentence, and paragraph movement/selection/deletion, copy, paste, cut, select all, save, undo, redo, bold, italic, underline, new document, open file, print, zoom in, zoom out, and reset zoom phrases
- visible window snap and Snap Layouts routing tests
- continuous pointer-motion routing tests for cardinal/diagonal directions, speed changes, explicit stop-moving behavior, and fixed-distance `move mouse <direction> <distance>` phrases
- pointer nudge, horizontal scroll, active-window continuous scrolling, and active-window scroll-edge routing tests for `start scrolling down`, `stop scrolling`, `scroll to top`, `scroll to bottom`, `scroll to left edge`, and `scroll to right edge`
- mouse button hold/release, triple-click, and direct mouse drag routing tests for visible drag workflows
- file result select/open/reveal routing tests plus blocked executable/script direct-open tests
- mouse grid routing, visible mouse-grid safety text, drag routing, and cell geometry tests

Manual parity coverage must include:

- clean install from the public website installer
- installed end-to-end automated checks against the built installer and installed runtime
- human-spoken core walkthrough covering wake, identity, overlay/readout, command capture, and visible action
- failure-state walkthrough covering wrong identity, timeout, cancel/reset, microphone/runtime failures, and safe recovery
- terminal-state authorization walkthrough proving completed, timed-out, cancelled, and locked-out sessions no longer accept command execution without a fresh wake plus identity turn
- clean Windows user or VM test proving the public installer works without developer state
- microphone setup and voice enrollment
- ambiguous app launch confirmation, including Voice Access-style bare-number/result phrases, next/previous choice movement, confirm-app confirmation, and cancel/reset clearing choices
- wrong identity, stale identity, timeout, and cancel flows
- show numbers/labels/grid on common Windows apps, including compact UI Automation-numbered controls in the foreground app, visible numbered-control safety text, click, toggle, expand, collapse, double-click, triple-click, and right-click by visible number or label, plus bounded slider movement on visible UIA sliders
- dictation into Notepad with review, visible review-buffer safety text, spoken target-text/range select/delete/replace, and correction
- dictation case-formatting in the review buffer before copy/paste
- dictation copy/paste review safety, including paste blocking for sensitive-looking foreground targets, failed paste or transfer preserving the visible review text, and local readback stop without review-buffer mutation
- correction alternatives for previous word/sentence/paragraph, including visible replacement-safety text, visible close button, cancel without mutation, and numbered choice
- browser navigation in Edge or Chrome, including visible browser safety text, home page navigation, fullscreen, private window launch, bookmark/favorites, print, and save-page actions, tab/window controls, visible page find with dictated search text, vertical/horizontal scrolling, and continuous browser scrolling with explicit browser-prefixed start/stop phrases
- app switching by next/previous window and by named open app/window with numbered multi-match confirmation, Task View, virtual desktop navigation, window management, snap left/right/up/down, and Snap Layouts
- visible Project and Cast panel opening without selecting a target display
- approval-gated clipboard history panel opening, verifying that Callsign does not read or store clipboard history contents
- approval-gated snipping toolbar opening, verifying that Callsign does not capture, read, save, or upload screenshots
- continuous pointer motion, visible mouse-grid safety text, speed changes, explicit stop-moving behavior, pointer nudges, mouse hold/release drag, direct mouse drag, and horizontal scrolling on a visible scrollable surface
- visible System safety text plus safe Windows Settings surfaces for display, sound, Bluetooth, Wi-Fi, network, accessibility, mouse, keyboard, privacy, power, installed apps, default apps, date/time, notifications, Windows Update, and personalization pages
- accessibility subpage walkthrough for Magnifier, Narrator, Captions, and Speech settings
- visible Magnifier open, zoom-out, and close flow
- safe media controls for play/pause, next track, previous track, and stop playback
- keyboard keypress and natural editing/formatting/document/zoom shortcut commands for Space, letter keys, number-row keys, symbol keys, Delete, Insert, Windows key, context menu key, Caps Lock, a function key, visible keyboard-overlay safety text, repeated single-key commands, repeated directional movement commands such as `move down two times`, selected-text boundary movement such as `move to beginning of selection` and `go to end of selection`, selected-text clearing with `unselect that` and `clear selection`, current word-boundary movement such as `go to beginning of word` and `move to end of word`, current-unit selection/deletion/copy/cut with `select word`, `select line`, `select paragraph`, `delete word`, `delete line`, `delete paragraph`, `copy word`, `copy line`, `copy paragraph`, `cut word`, `cut line`, and `cut paragraph`, previous/next copy-cut aliases such as `copy previous word`, `copy next line`, and `cut previous paragraph`, counted directional navigation such as `move left five characters`, `move right three words`, `go down three lines`, and `go back two paragraphs`, counted character, word, line, and paragraph copy/cut such as `copy previous three characters`, `copy last four characters`, `copy backward four characters`, `copy forward six letters`, `copy previous three words`, `copy next five words`, `copy previous three lines`, `copy backward three lines`, `copy next five lines`, `copy previous three paragraphs`, `copy next three paragraphs`, `cut forward six letters`, `cut forward four lines`, `cut next five words`, `cut next five lines`, and `cut next three paragraphs`, active-app unit formatting such as `bold previous word`, `italicize next line`, `underline paragraph`, and `make last paragraph underlined`, `dismiss` as an Escape alias, safe modifier chords (including allowlisted tab/home/end and Ctrl/Alt/Shift combinations), held Shift/Control/Alt with release-all safety, active-app previous/next character movement, character selection/deletion, line start/end movement, previous/next line movement, line selection/deletion, word/sentence/paragraph movement and selection, paragraph boundary selection/deletion, copy, paste, cut, select all, save, undo, redo, bold, italic, underline, new document, open file, visible print dialog, zoom in, zoom out, and reset zoom in a visible local app
- Explorer-backed file search, visible file-search safety text, numbered result selection, safe open, blocked executable direct-open, and reveal
- `what can I say` command discovery, including quick filters for all/Free/open-core/safety/dictation/visible-controls/extensions, search, selected-command details, risk/examples, open-core commands, extension commands, visible close-button dismissal, and voice dismissal
- Free parity versus paid-pack gating walkthrough, including the `Free Parity` command-palette filter, paid/Pro entitlement search, Plans and Packs surfaces, and visible proof that paid commands are listed for discovery only and will not route without entitlement while policy and visibility gates still apply after entitlement
- community extension import through the Packs UI, including visible full-tab and drop-zone import for normal Windows DLL filenames or folders, disabled-by-default state, enablement-readiness review for signature/entitlement/metadata/file blockers, metadata/signature/entitlement review, enable, disable, remove, and reimport/rollback behavior
- local voice shortcuts create/manage/execute walkthrough, including visible safety text, command steps, wait steps, unresolvable-step rejection before save, direct and indirect loop rejection, enable/disable, paid-entitlement/policy gate inheritance, service-side execution from the background runtime, and visible execution through existing Callsign surfaces
- update splash from a manifest that includes added, changed, and removed commands, feature highlights, or extension-pack changes, including the feature-only manifest case, spoken summary, visible details, accessible voice-dismissal cue, policy/entitlement reminder, and voice dismissal
- update restart proof for pending manifests and downloaded installers, including the visible restart-safe downloaded-installer path after a service restart
- startup walkthrough with a visible release-proof reminder, including the public installer and website download check before release

## Release gate

No `v1.0 alpha` release until:

- build site and installer are reproducible,
- service/session fields are readable in runtime state,
- overlay is visible at wake and hidden in terminal states,
- identity failures remain safe and visible,
- common app launches work through visible flow,
- and docs/website accurately describe open-source Free plus future closed-source extensions.

### Parity gate

`1.0.0a` is the first release that claims practical Windows 11 Voice Access parity.
No release can claim parity until every row in `VOICE_ACCESS_PARITY_MATRIX.md` is `Done` and automation + manual evidence is attached to each row.
Run `.\scripts\voice_access_parity_evidence.ps1` before any parity claim, or use `.\scripts\prepare-release-packet.ps1` to run the release-readiness build/site steps plus the parity smoke/template flow together. The generated JSON separates local evidence success from release proof: `passed` means the automated/documentation checks completed, while `release_ready` is true only when manual/live parity evidence is supplied and no evidence checks fail. It reports `canonical_manual_evidence.categories_missing` to prove the generated manual walkthrough template covers every parity category, and `manual_evidence.categories_covered` / `manual_evidence.categories_missing` to show which categories have actual submitted live evidence. Use `-RequireManualEvidence` on the parity or packet scripts when validating a release candidate so missing live walkthrough evidence fails the gate instead of being treated as implied completion. Manual evidence must contain passed named checks for clean install from the public installer, installed end-to-end automated checks, human-spoken core walkthrough, failure-state walkthrough, clean Windows user or VM test, enrollment, identity failure/cancel/reset, show numbers/grid, dictation, browser, window/settings, keyboard/mouse/media/file workflows, command discovery, browser overlay helper discovery from the walkthrough, local voice shortcuts create/manage/execute, community extension import/manage, release evidence folder access, update splash and imported-pack splash replay, visible Updates-status readback, visible visual-status readback, Apple Voice Control-style visual polish across the core visible surfaces, compact STOP badges and stop/cancel boundary copy on the wake overlay, visible-controls HUD, mouse grid, command palette, startup walkthrough, update splash, and import splash, and public website installer hash comparison. A passed `apple_style_visual_polish_walkthrough` must also complete the generated accessibility visual audit for keyboard-only operation, logical tab order, visible focus, screen-reader labels, no color-only state, high contrast, 200% text scaling, reduced motion, multi-monitor/DPI behavior, no-audio fallback, visible stop/cancel affordances, and non-focus-stealing overlays. `.\scripts\voice_access_parity_evidence.ps1 -WriteManualEvidenceTemplate` now also writes `build/voice-access-parity-manual-evidence.checklist.md` and `build/manual-proof-notes/*.md` so the live walkthrough can be followed as a human-readable checklist with one per-check note beside the canonical JSON template.
To prepare that file, run `.\scripts\voice_access_parity_evidence.ps1 -WriteManualEvidenceTemplate`. The generated canonical template is written to `build/voice-access-parity-manual-evidence.template.json` and includes every required check id, a description, a concrete `evidence_command` walkthrough prompt, an `expected_result` proof target, `parity_categories`, the local installer hash and byte size, and placeholders for public website download hash, public website byte size, machine details, operator, timestamps, observed result, artifact references, privacy review, and notes. Strict validation requires the supported manual-evidence schema, unique manual check ids, valid evidence timestamps, a public `Callsign-Setup.exe` download URL, test machine, Windows version, Callsign version, a timestamp, operator, environment, and notes for every manual check. It also requires the manual local and website installer SHA-256 values and byte sizes to match the current local `Callsign-Setup.exe`. Each manual check's `description`, `evidence_command`, `expected_result`, and `parity_categories` must also match the generated canonical template, so stale copied evidence cannot silently satisfy a newer parity walkthrough. Any check marked `passed` must include an `observed_result`, at least one `artifacts` reference such as a screenshot, video, transcript, audit log, hash output, or manual test note file, and a completed `privacy_review` checklist confirming no raw audio, no personal transcript, redacted paths/usernames, no tokens/secrets, and reviewed screenshots. Artifact references must be HTTP(S) URLs with evidence-file extensions such as `.png`, `.mp4`, `.json`, `.jsonl`, `.log`, `.md`, `.txt`, `.pdf`, or `.zip`, or existing local files inside the Callsign workspace using either absolute paths under the repo or repo-relative paths.
The generated canonical JSON and human-readable checklist both begin with the documentation-pack evidence header for commit, build ID, artifact hashes, tester, Windows build, machine or VM, microphone, install mode, UI/runtime versions, and wake/identity/transcription models before the per-check walkthrough prompts. Submitted manual evidence must fill that `evidence_header`, including a valid tested UTC timestamp with UTC offset zero and artifact hashes that name the current local `Callsign-Setup.exe` SHA-256, before the release gate can pass. Each checklist item also carries pass/fail/blocked, observed-result, evidence-path, sensitive-data review, remaining-uncertainty, and release-recommendation fields so the human notes mirror the strict JSON validation contract.

For a release candidate, prefer `.\scripts\prepare-release-packet.ps1` so the build/site verification, parity evidence JSON, canonical manual-evidence template, alpha walkthrough evidence JSON, website installer verification, and `build/release-packet-summary.json` are produced together from one command. Pass the public website URL plus the known website installer hash and size so the template and summary carry the same release metadata the verifier uses. The alpha walkthrough checklist writes `build/alpha-v1-walkthrough-evidence.json`, and the packet summary now records that file path plus a walkthrough discovery summary, the parity evidence `release_blocker_summary`, and the documentation-pack release gates for installed end-to-end automation, human-spoken core walkthrough, failure-state walkthrough, and clean Windows user or VM proof so the scripted walkthrough proof stays linked to the release packet. `.\scripts\alpha_v1_checklist.ps1 -Verify` now also expects the generated `build/voice-access-parity-manual-evidence.checklist.md` companion, validates that the release packet carries the structured parity blocker summary with blocker count, next action, manual-check count, and automated-failure count, and copies that summary into `build/alpha-v1-walkthrough-evidence.json` so the manual proof pass stays human-readable and machine-checkable. `.\scripts\verify-release-readiness.ps1` validates the manual evidence template schema, evidence header, at least 40 manual/live checks, checklist content, at least 40 checklist sections, and the `build/manual-proof-notes` folder with at least 40 markdown notes whenever the manual evidence template exists; each note must keep the required evidence command, expected result, observed result, artifact reference, privacy review, uncertainty, and release recommendation sections. If the Pi is unavailable, `-LocalPreviewOnly` keeps the same packet flow alive against the local website container, still verifies the local `/downloads/Callsign-Setup.exe` endpoint, records the preview installer hash and size plus a `release_mode` of `local-preview` in the packet summary, and avoids rebuilding the site twice by skipping the second site build in the preview deploy helper.

