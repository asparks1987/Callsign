# Voice Access Parity Matrix

## Definition

Callsign reaches practical Windows Voice Access parity when a user can control common Windows desktop work by voice with the same useful command families as stable Windows 11 Voice Access, while Callsign still requires:

```text
Callsign -> identity verification -> command -> visible action
```

The parity baseline is stable Windows 11 Voice Access. Legacy Windows Speech Recognition macros and Copilot voice/vision actions are not part of the Free parity requirement.

All parity features are Free/MIT open-core features through Alpha v1.

## Non-negotiable Callsign differences

- Callsign wake comes from the audio wake detector, not transcript text alone.
- The post-wake identity utterance is identity-only.
- Commands run inside a short visible verified session.
- Higher-risk commands can require approval or a fresh identity check.
- Every command remains policy-gated and auditable.
- The user must see listening, transcript/readout, pending command, action, and stop/cancel state.

## Parity categories

| Category | Release target | Status | Acceptance criteria | Verification |
|---|---|---|---|---|
| App launch | v1.0 | Done | Launch installed apps through visible Start menu flow; ambiguous matches must show visible numbered choices and require confirmation before execution, including Voice Access-style follow-ups such as `1`, `click 1`, `choose result 1`, `confirm app`, and `cancel`. Basic Free app launch rejects paths, URLs, shell text, secrets, elevated/admin wording, installer or uninstaller wording, and security tooling/settings intents; those must use explicit future policy-gated surfaces instead of the open-core launcher. Shell-backed fallback remains available only when Windows refuses to open Start/Search and is not counted as visible Start menu proof. Successful launch audit records must include the actual launch path plus visible Start menu verification when that path is used. | Alpha smoke resolver/ambiguity/audit tests plus unsafe-launch validation for elevated, installer, uninstaller, and security phrases, plus live `--live-launch Notepad` and `--live-launch Calculator`, both reporting `LaunchPath=start-menu-search`, `StartMenuOpened=True`, and `ShellFallbackUsed=False`. |
| Voice access controls | v1.2 | Done | Start/stop listening, including Voice Access-style `wake up`, `voice access wake up`, `unmute microphone`, `go to sleep`, `voice access sleep`, `turn off microphone`, `turn off voice access`, `stop voice access`, `close voice access`, `exit voice access`, and `quit voice access` listener-control phrases that do not replace audio-detector wake; show help; open the visible command palette with exact help/list aliases such as `show all commands`, `show command list`, and `open voice access help`; open an accessible clean-install walkthrough with `open voice access guide`; cancel or reset safely with urgent phrases such as `stop`, `stop now`, `pause`, `cancel`, and `reset session`; read back the current visible status, voice mode, evidence status, visual status, local time/date, and last-heard transcript locally with `what did you hear`, `read status`, `read voice mode status`, `read evidence status`, `read visual status`, `read visual polish status`, `what time is it`, `what's today's date`, or `repeat status`; stop that status readback with `stop status readback` or `stop reading status`; clear visible recent speech and the runtime transcript-history snapshot with `clear recent speech` or `clear speech history`; and switch between commands-only, dictation-only, and default command-plus-dictation modes with natural aliases such as `start command mode`, `start dictation mode`, `typing mode`, `turn off dictation mode`, and `commands plus dictation mode` without bypassing wake, identity, policy, visibility, or audit. | Router, verified-transcript, command-discovery, startup-walkthrough accessibility, and UI action smoke tests; manual walkthrough still required for release proof. |
| Voice shortcuts | v1.4 | Done | Open a visible local shortcuts surface with phrases such as `open voice shortcuts`, create/update/delete/enable/disable saved shortcuts, and run enabled shortcuts by voice. Shortcuts are local Free features stored on-device, appear in command discovery like a built-in pack, and can compose one to eight existing Callsign commands plus bounded wait steps without introducing arbitrary shell execution or bypassing wake, identity, policy, visibility, approval, or audit. Shortcut command steps must resolve through the built-in router or command-pack registry before save, direct self-recursion and indirect shortcut loops are rejected, malformed local shortcut data is filtered before registry exposure, and every step routes back through the normal visible command pipeline so browser, dictation, system, file, extension-pack, and Start menu actions still show their usual surfaces and policy gates when triggered from a shortcut. Follow-up routing must preserve the active profile's configured wake word instead of assuming the default `Callsign` wake cue. The background service consumes shortcut follow-up steps too, keeps the verified session alive only for the bounded chain, requests the visible Shortcuts surface, blocks recursive extension loops, and clears authorization when the chain completes. | Shortcut store/validation tests, indirect-loop rejection tests, malformed-local-data filtering tests, registry/discovery/follow-up-step smoke, configured-wake follow-up routing smoke, service-side follow-up routing smoke, verified-session routing smoke, and manual shortcut create/manage/execute walkthrough. |
| App switching | v1.3 | Done | Switch forward/backward across visible windows, switch directly to a named open app or window with phrases such as `switch to Edge` or `go to Notepad`, require visible numbered choice confirmation when multiple open windows match the same spoken target, open Task View with natural aliases such as `show all windows` and `window switcher`, open visible Quick Settings, Notification Center, display projection/cast, and emoji/symbol picker shell surfaces, and move to next/previous virtual desktop without hidden automation. | System command tests, visible window-resolution tests, and live app-switch walkthrough. |
| Window management | v1.3 | Done | Minimize, maximize, restore, close, snap left/right/up/down, show Windows 11 Snap Layouts, show desktop, and open Task Manager through visible local actions. | System route and dry-run command tests cover close-window, snap, minimize/maximize/restore, Task View, desktop, and Task Manager actions; manual verification remains required for release proof. |
| Visible control numbers | v1.2 | Done | Say `show numbers`, `show labels`, `show names`, `show numbers here`, `show numbers everywhere`, `show numbers on <app name>`, `show labels on <app name>`, `show names for <app name>`, `show control numbers in <app name>`, `show labels on taskbar`, `show numbers on taskbar`, `show buttons`, or `show click targets`, inspect foreground-app UIA controls, visible named-window controls, visible taskbar controls, or Callsign setup controls, show compact macOS-style numbered badges/HUD with accessible cue/transcript/focus/target metadata, move the visible focus cue through controls or fields with phrases such as `move to next field`, `tab forward`, `move to previous field`, and `tab backward`, activate or focus by number, label, field name, or selected visible control, explicitly toggle, expand, or collapse visible controls by number or spoken label with phrases such as `toggle number one`, `toggle wifi`, `flip dark mode`, `expand settings`, `open dropdown font`, `collapse settings`, and `close menu account`, perform visible double-click/triple-click/right-click actions by number or label when mouse semantics are needed, and move visible UIA sliders with bounded phrases such as `move slider right two times`, `move volume slider up 3 times`, and `adjust slider down one step`. Number and focus phrases include natural Voice Access-style variants such as `click number one`, `choose item twenty one`, `double click control twelve`, `triple click control twelve`, `right click option third`, `focus username field`, `go to address field`, `go to field subject`, `show labels on Notepad`, and `show numbers on Notepad`. | UIA label tests, numbered/focus/toggle/expand-collapse/slider phrase routing tests, overlay layout/accessibility tests, and manual common-app walkthrough. |
| Mouse grid | v1.2 | Done | Show a screen grid for coarse-to-fine mouse targeting plus click and drag actions, with accessible cue metadata for the current targeting options and a compact visible `STOP` badge in the grid status strip so stop/cancel remains obvious while targeting. Natural cell phrases include `grid five`, `select cell third`, `select square third`, `click cell one`, `click square one`, `drag from cell one to cell ninth`, and `drag from square one to square ninth`. Overlay aliases include `show grid everywhere`, `show mouse grid everywhere`, `show window grid`, and `show mouse window grid`, while `show grid here` or `mouse grid here` scopes the overlay to the current visible foreground window. Multi-display routing supports display identifiers such as `grid bravo` or `mouse grid alpha`, plus shortcut display paths such as `mouse grid A 114` for coarse-to-fine targeting across multiple monitors and current-scope multi-step shortcuts such as `mouse grid 114` or `mouse grid 1 1 4`. Voice Access-style grid control also includes `undo` / `undo that` to revert the previous grid state and `mark` / `mark four` followed by `drag` for visible marked drag-and-drop. | Grid geometry/router/accessibility tests plus manual grid walkthrough on multi-window desktop. |
| Mouse and scrolling | v1.2 | Done | Click, tap, left-click, double-click, triple-click, hold/release the mouse button with explicit aliases such as `hold mouse button`, `hold down mouse button`, `click and hold mouse button`, `press and hold mouse button`, `hold left mouse button`, `release left mouse button`, and `let go of mouse button`, start visible continuous pointer motion with `move mouse <direction>`, `move pointer <direction>`, or `move cursor <direction>` across cardinal and diagonal directions, adjust motion speed with `move faster` / `move slower`, stop motion with `stop moving`, move by a bounded fixed distance with phrases such as `move mouse left five` or `move cursor down left three`, drag the pointer a bounded distance up/down/left/right/diagonally with mouse/pointer/cursor aliases such as `drag pointer upper left`, scroll the visible pointer target with explicit mouse-wheel phrases such as `mouse scroll down a little`, scroll horizontally with `scroll left` and `scroll right`, start or stop continuous active-window scrolling with Voice Access-style phrases such as `start scrolling down`, `start scrolling left`, and `stop scrolling`, move the active visible app to edges with phrases such as `scroll to top`, `scroll to bottom`, `scroll to left edge`, and `scroll to right edge`, nudge the pointer up/down/left/right with short phrases such as `nudge up`, and keep status visible. Plain page-style phrases such as `scroll down a little` remain browser/page scroll commands, while plain `stop` remains the session-safety cancel phrase. | System route, verified-transcript, discovery, dry-run command tests, and manual verification. |
| Keyboard commands | v1.3 | Done | Press common keys, letter keys, number-row keys, symbol keys, arrows, navigation keys, Space, Backspace, Delete, Insert, Windows key, context menu key, Caps Lock, F1-F12, safe allowlisted modifier chords (Shift/Ctrl/Alt key combinations), repeated single-key presses with phrases such as `press tab five times`, `press return three times`, `tap down three times`, and `press down three times`, Voice Access-style repeated arrow movement with phrases such as `move down two times` and `go right four times`, and bounded held modifiers for Shift, Control, and Alt with `release all modifiers` as the safety release command. Natural key phrases include exact bare named keys such as `enter`, `return`, `tab`, `escape`, `space`, `delete`, `home`, `end`, `up`, `down`, `left`, `right`, `page up`, and `page down`, while bare letters and digits still require explicit phrases such as `press A` or `press 5`. Additional aliases include `hit enter`, `press return`, `tap tab`, `escape key`, `backspace`, `page down key`, `function five`, `tap f5`, `tap question mark`, `open start`, `show start menu`, and `windows search`. Natural shortcut phrases include `press shift a`, `press shift 1`, `press control c`, `press control v`, `press control s`, `press control f`, `press control r`, `press control l`, `press control w`, `press control 1`, `press control 9`, `press control shift t`, `press control shift n`, `press control shift 1`, `press alt shift tab`, `press alt f`, `press alt 1`, `hold shift`, `press and hold control key`, `dismiss`, `release`, `release alt`, `release all modifiers`, `press control plus`, `press control minus`, `press control zero`, Voice Access-style selected-text boundary movement with `move to beginning of selection` and `go to end of selection`, selected-text clearing with `unselect that` and `clear selection`, current-unit selection/deletion/copy/cut with `select word`, `select line`, `select paragraph`, `delete word`, `delete line`, `delete paragraph`, `copy word`, `copy line`, `copy paragraph`, `cut word`, `cut line`, and `cut paragraph`, and Voice Access-style document movement/selection aliases such as `go to start of document`, `go to end of document`, `select to beginning of document`, and `select to end of document`, plus natural safe editing/formatting/document/zoom shortcuts including copy, `copy that`, paste, `paste that`, `paste here`, cut, `cut that`, select all, `highlight all`, `select everything`, save, `save that`, `save this`, `save changes`, `save current document`, visible Save As dialog opening with `save as` and `open save as dialog`, undo, redo, `redo that`, `do that again`, bold, `bold that`, `make that bold`, italic, `italicize that`, `make that italic`, underline, `underline that`, `make that underlined`, app-scoped Find with `find in this app`, `search this app`, `find next in app`, and `find previous in app`, character movement/selection/deletion/copy/cut including counted and direction-alias phrases such as `last character`, `select last character`, `move previous five characters`, `move backward five characters`, `move left five characters`, `previous six letters`, `last six letters`, `forward six letters`, `right six letters`, `select previous four characters`, `select backward four characters`, `highlight next eight chars`, `remove forward twenty characters`, `copy previous three characters`, `copy last four characters`, `copy backward four characters`, and `cut forward six letters`, active-app line start/end movement, line movement/selection/deletion including counted and direction-alias phrases such as `move previous three lines`, `go up three lines`, `go back three lines`, `go down three lines`, `previous four lines`, `select previous two lines`, `select backward two lines`, `highlight forward five lines`, `highlight next five lines`, and `remove next ten lines`, word movement/selection/deletion including natural last-unit aliases such as `go to last word`, `select last word`, and `delete last word` plus counted phrases such as `move previous three words`, `move left three words`, `move right three words`, `previous four words`, `select previous two words`, `highlight next five words`, and `remove next ten words`, sentence movement/selection/deletion including natural last-unit aliases such as `go to last sentence`, `select last sentence`, and `delete last sentence` plus bounded counted phrases such as `move previous two sentences`, `next three sentences`, `highlight next four sentences`, and `remove previous five sentences`, paragraph boundary movement plus paragraph movement/selection/deletion including natural last-unit aliases such as `go to last paragraph`, `select last paragraph`, and `delete last paragraph` plus bounded counted phrases such as `move previous two paragraphs`, `go up two paragraphs`, `go back two paragraphs`, `select next three paragraphs`, `select down three paragraphs`, and `delete previous three paragraphs`, new document/new file with `start new document` and `make new file`, open-file dialog with `open existing file` and `open saved document`, visible print dialog with `print that` and `print this`, zoom in with `larger` and `increase text size`, zoom out with `smaller` and `decrease text size`, reset zoom with `actual size`, `default size`, and `normal size`, and visible emoji/symbol picker opening by voice. `show keyboard` opens a visible macOS-style on-screen keyboard overlay with accessible cue metadata, and `hide keyboard` dismisses it. | System, keyboard parser, keyboard overlay geometry/accessibility, command-discovery, and dry-run tests. |
| Dictation review | v1.1 | Done | Capture dictated text into a review surface before insertion/copy/paste, including direct text-entry phrases such as `type hello world`, `write hello world`, `insert text hello comma world`, `dictate alpha new line bravo`, `literal comma`, and `numeral twenty one`; spell words, letters, and symbols into the visible review buffer with natural phrases such as `spell it out w o m p r a t`, `spell that w o m p r a t`, `spell that out capital alpha bravo`, `spell the word womprat`, `spell alpha bravo charlie`, and `spell support at sign example dot com`; start and resume with natural aliases such as `start typing`, `start taking dictation`, and `resume typing`; add short local profile vocabulary entries with Voice Access-style phrases such as `add womprat to vocabulary`, `add word womprat to vocabulary`, `add phrase project zephyr to vocabulary`, `add to vocabulary womprat`, and `add project zephyr to dictation vocabulary`; toggle local dictation options with phrases such as `turn on fluid dictation`, `turn off fluid dictation`, `turn on automatic punctuation`, `auto punctuation on`, `turn off auto punctuation`, `turn on profanity filter`, and `do not filter profanity`, with those settings shaping newly captured review text and service-fed review refreshes locally; support local correction rollback phrases such as `revert` and `undo that` through the visible dictation undo path; read selected or reviewed text aloud locally with `read dictation`, `read that back`, or `speak text` before external text actions; cancel local readback with `stop reading`, `stop readback`, or `stop speaking` without changing reviewed text; surface visible correction and formatting launchers on the Dictation tab so the user can choose scope, open correction alternatives, and apply visible case formatting without voice alone; pause and resume dictation without clearing reviewed text, including `pause typing`, `pause voice typing`, `stop taking dictation`, and `stop voice typing`, with verified-session dictation commands routed through policy first and paste blocked for obvious password, credential, 2FA, wallet, payment, message compose, chat, upload, form, submit, post, checkout, and known external communication processes such as Outlook, Teams, Slack, Discord, Zoom, Webex, Signal, Telegram, WhatsApp, and Thunderbird. Local and service-fed dictation stop visibly at a shared 10-minute capture window, 12,000 reviewed characters, and 128 service segments while preserving review text; any failed paste or transfer preserves the visible review text and tells the user they can retry, copy, edit, or discard it. The background service evaluates built-in dictation policy with session-derived identity/freshness, requests the visible Dictation surface, and fails closed if fresh identity is missing. | Dictation vocabulary/profile smoke, dictation capture-bound smoke, dictation option route/profile/behavior smoke, spelling parser/discovery smoke, dictation readback parser/UI/discovery smoke, sensitive/external-target title and process paste-block tests, review-buffer preservation smoke, service-side built-in policy smoke, verified-session routing smoke, and Notepad manual walkthrough. |
| Text editing | v1.1 | Done | Select and delete the last dictated phrase with `select that`, `delete that`, `scratch that`, and `delete all`; select/delete/replace/insert around spoken target text inside the visible review buffer with phrases such as `select privacy policy`, `go before privacy policy`, `move after the phrase privacy policy`, `insert safety notes before privacy policy`, `insert comma after privacy policy`, `delete privacy policy`, `replace privacy policy with safety notes`, `replace privacy policy with hello comma world`, `fix privacy policy with safety notes`, `select from privacy to section`, `replace from privacy to section with safety notes`, and `replace from privacy to section with alpha new line bravo`; select and delete previous/next characters, lines, words, sentences, and paragraphs, including natural last-unit aliases such as `select last word`, `delete last sentence`, and `select last paragraph`; move/select/delete to the beginning or end of the reviewed text with natural phrases such as `go to beginning of text`, `select to beginning of text`, `go to end of text`, and `delete to end of dictation`; replace, move by line/word/sentence/paragraph with natural alternatives such as `fix previous word with hello` and `fix that with hello comma world`, with replacement text using the same visible dictation punctuation/new-line/literal/numeral conversion path as direct dictation; apply scoped and target-text case formatting in the visible review buffer with phrases such as `make all text title case`, `capitalize privacy policy`, and `make privacy policy title case`, set persistent casing for newly dictated text with `caps on`, `all caps on`, `no caps on`, and `caps off`, insert or remove spaces with `space` and `no space that`, insert `tab`, `new sentence`, `new line`, and `new paragraph` breaks, wrap selected or recent dictation with quotes, parentheses, brackets, or braces, insert punctuation plus common symbols including natural aliases such as `exclamation`, `semi colon`, `open parentheses`, `open square bracket`, brackets, braces, slash, backslash, pipe, backtick, tilde, underscore, plus, equals, number sign, dollar sign, ampersand, percent, caret, asterisk, and at sign, and recover review-buffer edits with explicit undo/redo snapshots. | Dictation parser/edit/formatting/symbol tests. |
| Correction alternatives | v1.1 | Done | Present correction choices before replacing dictated text in the visible review surface, with a compact macOS-style correction HUD that shows scope, selected numbered choice, and spoken choice/cancel cues. Natural correction aliases include `fix that`, `fix previous word`, and `fix all text`. | Dictation correction parser/service/UI tests and manual walkthrough. |
| Browser navigation | v1.2 | Done | Open/search/navigate, validated visible address-bar target entry, natural address-bar dictation such as `type example.com in the address bar`, history navigation with page-scoped aliases such as `go back`, `back a page`, `go back a page`, `previous web page`, `go forward`, `forward a page`, `go forward a page`, and `next web page`, refresh/reload with `refresh this page` and `reload current page`, scoped page-load cancellation with `stop loading page`, `browser stop loading`, or `cancel page load`, home page navigation with scoped aliases such as `browser home`, `browser go home`, `browser go to home`, `go to home page`, or `open browser home page`, scoped current-page address copying with `copy page address`, `copy current page url`, or `browser copy address`, fullscreen, private window launch, bookmark/favorites, downloads/history utility pages with aliases such as `open favorites page`, `open downloads page`, or `open history page`, print, and save-page actions, tab/window controls including `browser duplicate tab`, `duplicate tab`, `open current page in new tab`, tab reordering with `move tab left`, `move current tab right`, or `browser move tab left`, and numbered tab selection with `go to tab one`, `switch to browser tab three`, or `browser tab nine`, page find with dictated search text including `find privacy policy on this page`, vertical and horizontal scroll, bounded page-count scrolling with `scroll down two pages`, `browser scroll down five pages`, and `page up twice`, browser-prefixed continuous page scrolling with `browser start scrolling <direction>` and `browser stop scrolling`, browser-local overlay launchers for `show numbers`, `show grid`, and `hide overlays`, page-style aliases such as `page down in browser`, `go to top of page`, and `go to bottom of page`, zoom including `browser larger`, `browser increase text size`, `browser smaller`, `browser decrease text size`, and natural reset aliases such as `browser actual size`, `browser normal size`, `browser default size`, or `browser default zoom`, and safe bounded actions. Direct browser opens and address-bar target entry are limited to http/https, domains, or search text; credential-bearing web addresses, user-info-shaped targets, and non-web URI schemes are blocked and routed to their proper visible command family instead, and plain `stop` remains the session-safety phrase rather than the browser-scroll stop command. External side-effect phrases such as `submit form`, `send message`, `upload file`, `post comment`, `pay now`, `accept terms`, `delete cloud data`, or `download and execute` are routed to a visible blocked action with audit instead of visible-control activation, browser fallback, or app launch. The background service evaluates built-in browser policy with session-derived identity/freshness before visible browser action, routes every `browser-*` action target through the same `BrowserLaunchService` action path as the visible Browser tab before any URL/search fallback, routes blocked submit/send/upload-style phrases to the visible denial path, and requests the visible Browser or safety surface. | Browser helper tests, blocked external-side-effect routing smoke, scrolling execution smoke, broad service-side browser-action smoke, service-side built-in policy smoke, live browser session probes, and Edge/Chrome manual walkthrough. |
| File search | v1.3 | Done | Search files/folders in user/profile-style roots, block system/program roots, select numbered results, and safely open or reveal results through visible Explorer-backed flow, with verified-session file commands routed through policy first. The background service evaluates built-in file-search policy with session-derived identity/freshness before searching, requests the visible Files surface, and reports blocked paths or no-result states visibly. Natural folder-search phrases include `find folder named invoices`, `search my folders for invoices`, and `look in folders for receipts`. Numbered result phrases include ordinal and compound spoken forms such as `open result eleventh`, `open result twenty one`, `choose result thirty second`, and `reveal result thirty nine`, plus containing-folder reveal phrases such as `open containing folder for result 1` and `show containing folder for result 2`. | File helper/router tests, service-side built-in policy smoke, verified-session routing smoke, and live Explorer reveal probe. |
| Safe system/settings control | v1.3 | Done | Volume, safe media controls, visible Windows Settings/accessibility surfaces, and reversible local actions only. Current commands cover volume/mute, play/pause, next/previous/stop media, visible Magnifier open/zoom-out/close controls, accessibility subpages for Magnifier/Narrator/Captions/Speech without toggling those features, including natural aliases such as `zoom settings`, `screen reader settings`, `live captions settings`, `voice access settings`, and `voice typing settings`; `open voice access settings` opens Callsign's visible Voice surface while the bare settings aliases continue to reach the Windows Speech settings page; approval-gated visible close-window requests with aliases such as `close active app` and `press alt f4` without force-killing hidden processes; approval-gated visible clipboard history with natural aliases such as `open clipboard` and `show clipboard picker` without reading clipboard contents; approval-gated visible snipping toolbar with natural aliases such as `show screenshot toolbar` and `open screenshot tools` without capturing/reading/saving/uploading screenshots; and Windows, display, sound, Bluetooth, Wi-Fi, network, accessibility, mouse, keyboard, privacy, power/battery, installed apps, default apps, date/time, notifications, Windows Update, and personalization settings without changing settings values, including Windows 11 category aliases such as `bluetooth and devices settings`, `network and internet settings`, `apps and features settings`, and `time and language settings`; verified-session system commands route through policy before visible execution. The background service executes safe system-control intents through the same `SystemControlService`, requests the visible System surface, derives identity and freshness from the active session, defers ambiguous named-window switching to visible numbered choices, and blocks approval-required system actions until the System tab can collect explicit approval. | Router smoke tests, policy tests, service-side system-control routing smoke, verified-session routing smoke, and manual verification. |
| Help and command discovery | v1.2 | Done | `what can I say` opens a searchable command palette with open-core and extension commands, risks, examples, result counts, safety commands, selected-command details, approval metadata, and accessible metadata for the core visible labels. The palette exposes a `Free Parity` filter for `free parity`, `voice access parity`, and `open core parity` searches so Windows Voice Access parity commands stay visibly Free/open-core before paid-pack discovery, and visible approval filters including `approval:visible choice`, `approval:fresh`, and `approval:none` so ambiguous visible-choice commands, fresh-identity commands, and approval-free commands can be reviewed before routing. The plain help text also includes compact `Try:` examples and inline source/tier/availability/approval metadata so natural phrases such as browser find, dictation fix, pointer nudge, and extension commands remain discoverable outside the full palette. | Voice help, command-palette filtering/accessibility tests, and manual command-palette walkthrough. |
| Extension packs | v1.0-v1.4 | Done | Built-in, community, and paid packs use the same registry, policy, disable, rollback, and update model. The registry validates required pack/command metadata, marks malformed packs `InvalidPack`, and prevents invalid packs from routing; defaults to Free-only entitlement; shows paid-pack and paid-command metadata as entitlement-required; blocks unentitled paid command routing even when a paid command is bundled inside a Free pack; blocks required-signature packs until signed/trusted; and enforces identity, fresh-identity, and approval policy again at the pack execution boundary so direct execution cannot bypass policy. Service-side extension execution must pass the active session's actual identity and fresh-identity state into the registry rather than hard-coding authorization, so stale, terminal, or profile-mismatched sessions fail closed at the pack boundary. Disabled, entitlement-gated, and signature-gated commands remain listed for discovery only, with availability text, selected-command details, and voice cue routing-gate language saying they will not route until the relevant enablement, entitlement, or signature gate is satisfied. The visible import splash can replay its narration, and the most recent import summary survives restart so imported pack changes remain readable before enablement. | Pack registry/import/metadata/entitlement/signature/policy-boundary tests, including service-side session-derived identity/freshness handoff, command-level entitlement gating, command-palette routing-gate tests, import-splash replay tests, and import-splash persistence tests. |
| Update splash | v1.4 | Done | App reads update manifest, shows newly added/changed commands, feature highlights, or extension-pack changes, and can be dismissed by voice without changing installed commands. The update check-in path keeps raw account, callsign, and device identifiers hashed before phone-home check-ins while preserving visible last-check, next-due, last downloaded installer, and release-proof status. | Manifest parsing tests, UI smoke, routing smoke, command discovery smoke, and update check-in privacy smoke. |

Keyboard parity note: the Keyboard commands row also covers Voice Access-style previous/next copy and cut aliases, including `copy previous word`, `copy next word`, `copy previous line`, `copy next line`, `copy previous paragraph`, `copy next paragraph`, `cut previous word`, `cut next word`, `cut previous line`, `cut next line`, `cut previous paragraph`, and `cut next paragraph`, plus counted directional navigation phrases such as `move left five characters`, `move right three words`, `go down three lines`, and `go back two paragraphs`, plus counted character, word, line, and paragraph copy/cut phrases such as `copy previous three characters`, `copy last four characters`, `copy backward four characters`, `copy forward six letters`, `cut forward six letters`, `copy previous three words`, `copy next five words`, `cut previous three words`, `cut next five words`, `copy previous three lines`, `copy backward three lines`, `copy next five lines`, `cut forward four lines`, `cut next five lines`, `copy previous three paragraphs`, `copy next three paragraphs`, and `cut next three paragraphs`. The active-app formatting slice also covers foreground unit formatting through visible keyboard semantics for phrases such as `bold previous word`, `italicize next line`, `underline paragraph`, and `make last paragraph underlined`. Text navigation includes explicit current word-boundary aliases such as `go to beginning of word`, `go to start of word`, `move to beginning of word`, and `move to end of word`.

## Command execution pipeline

Every command family must use this pipeline:

```text
speech transcript
-> normalized intent
-> command registry resolution
-> policy evaluation
-> approval or fresh identity if required
-> visible execution
-> verification
-> audit log
```

No command pack can bypass wake, identity, policy, visibility, or audit.
Identity authorization is valid only while the state machine is actively waiting for a command, ready to launch, or visibly launching. Completed, cancelled, timed-out, and locked-out terminal states must clear verified identity and pending command/app data so stale authorization cannot be reused after the short verified session ends.
Audit records include a correlation id plus a compact verification object so visible execution can be tied back to the command path that produced it. Built-in policy audit records mark `policy_evaluation` verification before allowed commands proceed. Built-in browser, dictation, extension-pack UI, file-search, help discovery, visible UI, voice control, update splash, and system execution records mark `visible_status` verification when the visible Browser, Dictation review/correction, Packs, Files, command palette/walkthrough, overlay/grid, voice-control status, update splash, or System status surface is updated. The background service also writes profile-scoped `alpha.service_command_execution` audit entries with `audit_source` set to `service_runtime` for service-executed browser, file-search, dictation, system, UI, extension, and Start-menu actions, so runtime snapshot history is backed by the same JSONL audit trail as visible UI commands. If the service cannot write that audit entry, the visible status and recent-action history include an `Audit warning` with bounded recovery guidance instead of suppressing the failure. Extension command audit uses one correlation id across registry resolution, policy or approval decisions, and pack execution, then records `registry_resolution`, `policy_evaluation`, `user_approval`, or `pack_execution` as the verification phase.

## Extension pack requirements

Built-in Free parity commands should be implemented as normal command packs where practical.

Community packs:

- import normal Windows `.dll` filenames through the Packs UI with `Import Pack...` or `Import Folder...`, or drag-and-drop one or more `.dll` files/folders into the full Packs tab or pack list,
- automatically discover newly dropped `.dll` packs in the watched Packs folder so imported commands appear without a manual refresh,
- show the visible import splash for newly discovered packs so their commands and features are announced before enablement, and preserve the most recent import splash summary so `read import summary again` can replay it after restart,
- start disabled by default when imported through the UI,
- must show command metadata, tier, signature status, entitlement status, and safety gates before enablement,
- can be disabled and re-enabled,
- must not bypass policy evaluation.

Future paid packs:

- are entitlement-gated for pack loading and command routing,
- are still policy-gated for execution,
- receive identity and fresh-identity proof from the active session state machine at execution time,
- must be signed, updateable, disableable, and rollback-capable.

## Release acceptance

No release can claim 100% Voice Access parity until:

- every parity category above is `Done`,
- automated smoke coverage passes,
- `.\scripts\voice_access_parity_evidence.ps1` writes structured evidence for the matrix, installer, generated parity page, and named manual/live proof status; `-WriteManualEvidenceTemplate` prepares the release walkthrough evidence file, including the import-splash replay walkthrough,
- a clean install walkthrough passes, exposes the Account, Voice, Session, Shortcuts, Plans, Updates, and Packs surfaces, and reaches the visible release-proof step before release,
- the latest installer is built,
- the public website serves the latest installer,
- and the parity matrix is regenerated into the public docs site.
