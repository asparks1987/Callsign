# Voice Access Parity Matrix

## Definition

Callsign reaches practical Windows Voice Access parity when a user can control common Windows desktop work by voice with the same useful command families as stable Windows 11 Voice Access, while Callsign still requires:

```text
Callsign -> identity verification -> command -> visible action
```

The parity baseline is stable Windows 11 Voice Access. Legacy Windows Speech Recognition macros and Copilot voice/vision actions are not part of the Free parity requirement.

All parity features are Free/MIT open-core features through Alpha v1.

## Non-negotiable Callsign differences

- Callsign wake comes from the audio wake detector, not transcript text alone.
- The post-wake identity utterance is identity-only.
- Commands run inside a short visible verified session.
- Higher-risk commands can require approval or a fresh identity check.
- Every command remains policy-gated and auditable.
- The user must see listening, transcript/readout, pending command, action, and stop/cancel state.

## Parity categories

| Category | Release target | Status | Acceptance criteria | Verification |
|---|---|---|---|---|
| App launch | v1.0 | Done | Launch installed apps through visible Start menu flow; ambiguous matches must show visible numbered choices and require confirmation before execution, including Voice Access-style follow-ups such as `1`, `click 1`, `choose result 1`, `confirm app`, and `cancel`. Shell-backed fallback remains available only when Windows refuses to open Start/Search and is not counted as visible Start menu proof. Successful launch audit records must include the actual launch path plus visible Start menu verification when that path is used. | Alpha smoke resolver/ambiguity/audit tests plus live `--live-launch Notepad` and `--live-launch Calculator`, both reporting `LaunchPath=start-menu-search`, `StartMenuOpened=True`, and `ShellFallbackUsed=False`. |
| Voice access controls | v1.2 | Done | Start/stop listening, including Voice Access-style `wake up`, `voice access wake up`, `unmute microphone`, `go to sleep`, `voice access sleep`, `turn off microphone`, `turn off voice access`, `stop voice access`, `close voice access`, `exit voice access`, and `quit voice access` listener-control phrases that do not replace audio-detector wake; show help; open the visible command palette with exact help/list aliases such as `show all commands`, `show command list`, and `open voice access help`; open an accessible clean-install walkthrough with `open voice access guide`; cancel or reset safely with urgent phrases such as `stop`, `stop now`, `pause`, `cancel`, and `reset session`; read back the current visible status and last-heard transcript locally with `what did you hear`, `read status`, or `repeat status`; stop that status readback with `stop status readback` or `stop reading status`; clear visible recent speech and the runtime transcript-history snapshot with `clear recent speech` or `clear speech history`; and switch between commands-only, dictation-only, and default command-plus-dictation modes with natural aliases such as `start command mode`, `start dictation mode`, `typing mode`, `turn off dictation mode`, and `commands plus dictation mode` without bypassing wake, identity, policy, visibility, or audit. | Router, verified-transcript, command-discovery, startup-walkthrough accessibility, and UI action smoke tests; manual walkthrough still required for release proof. |
| Voice shortcuts | v1.4 | Done | Open a visible local shortcuts surface with phrases such as `open voice shortcuts`, create/update/delete/enable/disable saved shortcuts, and run enabled shortcuts by voice. Shortcuts are local Free features stored on-device, appear in command discovery like a built-in pack, and can compose one to eight existing Callsign commands plus bounded wait steps without introducing arbitrary shell execution or bypassing wake, identity, policy, visibility, approval, or audit. Shortcut steps must route back through the normal visible command pipeline, so browser, dictation, system, file, extension-pack, and Start menu actions still show their usual surfaces and policy gates when triggered from a shortcut. | Shortcut store/validation tests, registry/discovery/follow-up-step smoke, verified-session routing smoke, and manual shortcut create/manage/execute walkthrough. |
| App switching | v1.3 | Done | Switch forward/backward across visible windows, switch directly to a named open app or window with phrases such as `switch to Edge` or `go to Notepad`, require visible numbered choice confirmation when multiple open windows match the same spoken target, open Task View with natural aliases such as `show all windows` and `window switcher`, open visible Quick Settings, Notification Center, display projection/cast, and emoji/symbol picker shell surfaces, and move to next/previous virtual desktop without hidden automation. | System command tests, visible window-resolution tests, and live app-switch walkthrough. |
| Window management | v1.3 | Done | Minimize, maximize, restore, close, snap left/right/up/down, show Windows 11 Snap Layouts, show desktop, and open Task Manager through visible local actions. | System route and dry-run command tests cover close-window, snap, minimize/maximize/restore, Task View, desktop, and Task Manager actions; manual verification remains required for release proof. |
| Visible control numbers | v1.2 | Done | Say `show numbers`, `show numbers here`, `show numbers everywhere`, `show numbers on <app name>`, or `show numbers on taskbar`, inspect foreground-app UIA controls, visible named-window controls, visible taskbar controls, or Callsign setup controls, show compact macOS-style numbered badges/HUD with accessible cue/transcript/focus/target metadata, move the visible focus cue through controls or fields with phrases such as `move to next field`, `tab forward`, `move to previous field`, and `tab backward`, activate by number, label, or selected visible control, and perform visible double-click/right-click actions by number or label when mouse semantics are needed. Number phrases include natural Voice Access-style variants such as `click number one`, `choose item twenty one`, `double click control twelve`, `right click option third`, and `show numbers on Notepad`. | UIA label tests, numbered phrase routing tests, overlay layout/accessibility tests, and manual common-app walkthrough. |
| Mouse grid | v1.2 | Done | Show a screen grid for coarse-to-fine mouse targeting plus click and drag actions, with accessible cue metadata for the current targeting options. Natural cell phrases include `grid five`, `select cell third`, `click cell one`, and `drag from cell one to cell ninth`. Overlay aliases include `show grid everywhere` and `show window grid`, while `show grid here` scopes the overlay to the current visible foreground window. Multi-display routing supports display identifiers such as `grid bravo` or `mouse grid alpha`, plus shortcut display paths such as `mouse grid A 114` for coarse-to-fine targeting across multiple monitors and current-scope multi-step shortcuts such as `mouse grid 114` or `mouse grid 1 1 4`. Voice Access-style grid control also includes `undo` / `undo that` to revert the previous grid state and `mark` / `mark four` followed by `drag` for visible marked drag-and-drop. | Grid geometry/router/accessibility tests plus manual grid walkthrough on multi-window desktop. |
| Mouse and scrolling | v1.2 | Done | Click, tap, left-click, double-click, triple-click, hold/release the mouse button, start visible continuous pointer motion with `move mouse <direction>` across cardinal and diagonal directions, adjust motion speed with `move faster` / `move slower`, stop motion with `stop moving`, move by a bounded fixed distance with phrases such as `move mouse left five`, drag the pointer a bounded distance up/down/left/right/diagonally, scroll the visible pointer target with explicit mouse-wheel phrases such as `mouse scroll down a little`, scroll horizontally with `scroll left` and `scroll right`, nudge the pointer up/down/left/right with short phrases such as `nudge up`, and keep status visible. Plain page-style phrases such as `scroll down a little` remain browser/page scroll commands, while plain `stop` remains the session-safety cancel phrase. | System route, verified-transcript, discovery, dry-run command tests, and manual verification. |
| Keyboard commands | v1.3 | Done | Press common keys, letter keys, number-row keys, symbol keys, arrows, navigation keys, Space, Delete, Insert, Windows key, context menu key, Caps Lock, F1-F12, safe allowlisted modifier chords (Shift/Ctrl/Alt key combinations), repeated single-key presses with phrases such as `press tab five times` and `press down three times`, and bounded held modifiers for Shift, Control, and Alt with `release all modifiers` as the safety release command. Natural shortcut phrases include `press shift a`, `press shift 1`, `press control c`, `press control v`, `press control s`, `press control f`, `press control r`, `press control l`, `press control w`, `press control 1`, `press control 9`, `press control shift t`, `press control shift n`, `press control shift 1`, `press alt shift tab`, `press alt f`, `press alt 1`, `hold shift`, `press and hold control key`, `dismiss`, `release`, `release alt`, `release all modifiers`, `press control plus`, `press control minus`, and `press control zero`, plus natural safe editing/formatting/document/zoom shortcuts including copy, paste, cut, select all, save, undo, redo, bold, `bold that`, `make that bold`, italic, `italicize that`, `make that italic`, underline, `underline that`, `make that underlined`, previous/next character movement, previous/next character selection/deletion, active-app line start/end movement, previous/next line movement, line selection, line deletion, word/sentence/paragraph movement and selection before generic visible-control label routing, paragraph boundary movement/selection/deletion, new document, open file, visible print dialog, zoom in, zoom out, reset zoom, and visible emoji/symbol picker opening by voice. `show keyboard` opens a visible macOS-style on-screen keyboard overlay with accessible cue metadata, and `hide keyboard` dismisses it. | System, keyboard parser, keyboard overlay geometry/accessibility, command-discovery, and dry-run tests. |
| Dictation review | v1.1 | Done | Capture dictated text into a review surface before insertion/copy/paste, including direct text-entry phrases such as `type hello world`, `write hello world`, `insert text hello comma world`, and `dictate alpha new line bravo`; start and resume with natural aliases such as `start typing`, `start taking dictation`, and `resume typing`; add short local profile vocabulary entries with Voice Access-style phrases such as `add womprat to vocabulary`, `add to vocabulary womprat`, and `add project zephyr to dictation vocabulary`; toggle local dictation options with phrases such as `turn on fluid dictation`, `turn off fluid dictation`, `turn on automatic punctuation`, `turn off automatic punctuation`, `turn on profanity filter`, and `do not filter profanity`, with those settings shaping newly captured review text and service-fed review refreshes locally; support local correction rollback phrases such as `revert` and `undo that` through the visible dictation undo path; read selected or reviewed text aloud locally with `read dictation`, `read that back`, or `speak text` before external text actions; cancel local readback with `stop reading`, `stop readback`, or `stop speaking` without changing reviewed text; pause and resume dictation without clearing reviewed text, including `pause typing`, `pause voice typing`, `stop taking dictation`, and `stop voice typing`, with verified-session dictation commands routed through policy first and paste blocked for obvious password/secret/payment targets. | Dictation vocabulary/profile smoke, dictation option route/profile/behavior smoke, dictation readback parser/UI/discovery smoke, sensitive-target paste-block tests, verified-session routing smoke, and Notepad manual walkthrough. |
| Text editing | v1.1 | Done | Select and delete the last dictated phrase with `select that`, `delete that`, `scratch that`, and `delete all`; select/delete/replace spoken target text inside the visible review buffer with phrases such as `select privacy policy`, `go before privacy policy`, `move after the phrase privacy policy`, `delete privacy policy`, `replace privacy policy with safety notes`, `fix privacy policy with safety notes`, `select from privacy to section`, and `replace from privacy to section with safety notes`; select and delete previous/next characters, lines, words, sentences, and paragraphs; move/select/delete to the beginning or end of the reviewed text with natural phrases such as `go to beginning of text`, `select to beginning of text`, `go to end of text`, and `delete to end of dictation`; replace, move by line/word/sentence/paragraph with natural alternatives such as `fix previous word with hello`, apply case formatting in the visible review buffer, set persistent casing for newly dictated text with `caps on`, `all caps on`, `no caps on`, and `caps off`, insert or remove spaces with `space` and `no space that`, insert `tab`, `new sentence`, `new line`, and `new paragraph` breaks, wrap selected or recent dictation with quotes, parentheses, brackets, or braces, insert punctuation plus common symbols including natural aliases such as `exclamation`, `semi colon`, `open parentheses`, `open square bracket`, brackets, braces, slash, backslash, pipe, backtick, tilde, underscore, plus, equals, number sign, dollar sign, ampersand, percent, caret, asterisk, and at sign, and recover review-buffer edits with explicit undo/redo snapshots. | Dictation parser/edit/formatting/symbol tests. |
| Correction alternatives | v1.1 | Done | Present correction choices before replacing dictated text in the visible review surface, with a compact macOS-style correction HUD that shows scope, selected numbered choice, and spoken choice/cancel cues. Natural correction aliases include `fix that`, `fix previous word`, and `fix all text`. | Dictation correction parser/service/UI tests and manual walkthrough. |
| Browser navigation | v1.2 | Done | Open/search/navigate, validated visible address-bar target entry, natural address-bar dictation such as `type example.com in the address bar`, home page navigation, fullscreen, private window launch, bookmark/favorites, print, and save-page actions, tab/window controls, page find with dictated search text including `find privacy policy on this page`, vertical and horizontal scroll, continuous page scrolling with `start scrolling <direction>` and `stop scrolling`, page-style aliases such as `page down in browser`, `go to top of page`, and `go to bottom of page`, zoom, and safe bounded actions. Direct browser opens and address-bar target entry are limited to http/https, domains, or search text; non-web URI schemes are blocked and routed to their proper visible command family instead, and plain `stop` remains the session-safety phrase rather than the browser-scroll stop command. | Browser helper tests, scrolling execution smoke, live browser session probes, and Edge/Chrome manual walkthrough. |
| File search | v1.3 | Done | Search files/folders in user/profile-style roots, block system/program roots, select numbered results, and safely open or reveal results through visible Explorer-backed flow, with verified-session file commands routed through policy first. Natural folder-search phrases include `find folder named invoices`, `search my folders for invoices`, and `look in folders for receipts`. Numbered result phrases include ordinal and compound spoken forms such as `open result eleventh`, `open result twenty one`, `choose result thirty second`, and `reveal result thirty nine`, plus containing-folder reveal phrases such as `open containing folder for result 1` and `show containing folder for result 2`. | File helper/router tests, verified-session routing smoke, and live Explorer reveal probe. |
| Safe system/settings control | v1.3 | Done | Volume, safe media controls, visible Windows Settings/accessibility surfaces, and reversible local actions only. Current commands cover volume/mute, play/pause, next/previous/stop media, visible Magnifier open/zoom-out/close controls, accessibility subpages for Magnifier/Narrator/Captions/Speech without toggling those features, including natural aliases such as `zoom settings`, `screen reader settings`, `live captions settings`, `voice access settings`, and `voice typing settings`; `open voice access settings` opens Callsign's visible Voice surface while the bare settings aliases continue to reach the Windows Speech settings page; approval-gated visible clipboard history with natural aliases such as `open clipboard` and `show clipboard picker` without reading clipboard contents; approval-gated visible snipping toolbar with natural aliases such as `show screenshot toolbar` and `open screenshot tools` without capturing/reading/saving/uploading screenshots; and Windows, display, sound, Bluetooth, Wi-Fi, network, accessibility, mouse, keyboard, privacy, power/battery, installed apps, default apps, date/time, notifications, Windows Update, and personalization settings without changing settings values; verified-session system commands route through policy before visible execution. | Router smoke tests, policy tests, verified-session routing smoke, and manual verification. |
| Help and command discovery | v1.2 | Done | `what can I say` opens a searchable command palette with built-in and extension commands, risks, examples, result counts, safety commands, selected-command details, and accessible metadata for the core visible labels. The plain help text also includes compact `Try:` examples so natural phrases such as browser find, dictation fix, pointer nudge, and extension commands remain discoverable outside the full palette. | Voice help, command-palette filtering/accessibility tests, and manual command-palette walkthrough. |
| Extension packs | v1.0-v1.4 | Done | Built-in, community, and future paid packs use the same registry, policy, disable, rollback, and update model. The registry validates required pack/command metadata, marks malformed packs `InvalidPack`, and prevents invalid packs from routing; defaults to Free-only entitlement; shows paid-pack and paid-command metadata as entitlement-required; blocks unentitled paid command routing even when a paid command is bundled inside a Free pack; blocks required-signature packs until signed/trusted; and enforces identity, fresh-identity, and approval policy again at the pack execution boundary so direct execution cannot bypass policy. | Pack registry/import/metadata/entitlement/signature/policy-boundary tests, including command-level entitlement gating. |
| Update splash | v1.4 | Done | App reads update manifest, shows newly added/changed commands or features, and can be dismissed by voice without changing installed commands. | Manifest parsing tests, UI smoke, routing smoke, and command discovery smoke. |

## Command execution pipeline

Every command family must use this pipeline:

```text
speech transcript
-> normalized intent
-> command registry resolution
-> policy evaluation
-> approval or fresh identity if required
-> visible execution
-> verification
-> audit log
```

No command pack can bypass wake, identity, policy, visibility, or audit.
Audit records include a correlation id plus a compact verification object so visible execution can be tied back to the command path that produced it. Built-in policy audit records mark `policy_evaluation` verification before allowed commands proceed. Built-in browser, dictation, extension-pack UI, file-search, help discovery, visible UI, voice control, update splash, and system execution records mark `visible_status` verification when the visible Browser, Dictation review/correction, Packs, Files, command palette/walkthrough, overlay/grid, voice-control status, update splash, or System status surface is updated. Extension command audit uses one correlation id across registry resolution, policy or approval decisions, and pack execution, then records `registry_resolution`, `policy_evaluation`, `user_approval`, or `pack_execution` as the verification phase.

## Extension pack requirements

Built-in Free parity commands should be implemented as normal command packs where practical.

Community packs:

- import through the Packs UI with `Import Pack...` or `Import Folder...`, or drag-and-drop one or more `.dll` files/folders into the pack list,
- start disabled by default when imported through the UI,
- must show command metadata, tier, signature status, entitlement status, and safety gates before enablement,
- can be disabled and re-enabled,
- must not bypass policy evaluation.

Future paid packs:

- are entitlement-gated for pack loading and command routing,
- are still policy-gated for execution,
- must be signed, updateable, disableable, and rollback-capable.

## Release acceptance

No release can claim 100% Voice Access parity until:

- every parity category above is `Done`,
- automated smoke coverage passes,
- `.\scripts\voice_access_parity_evidence.ps1` writes structured evidence for the matrix, installer, generated parity page, and named manual/live proof status; `-WriteManualEvidenceTemplate` prepares the release walkthrough evidence file,
- a clean install walkthrough passes,
- the latest installer is built,
- the public website serves the latest installer,
- and the parity matrix is regenerated into the public docs site.
